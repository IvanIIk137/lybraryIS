import 'package:get/get.dart';
import 'package:{{> projectName }}/controllers/{{ dir }}/{{ snakeCase name }}_controller.dart';

class {{ properCase name }}Binding implements Bindings {
  @override
  void dependencies() {
    Get.put<{{ properCase name }}Controller>({{ properCase name }}Controller());
  }
}